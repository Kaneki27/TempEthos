rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allow public read/write access during development and testing
    // since authentication is handled via collection-based lookups
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
